**Making new repository for labs**
- make new repo in github
- copy url
- go to folder where files are located
- run `git init`, 'git branch -M main', and 'git remote add origin <PASTE_YOUR_REPOSITORY_URL_HERE>'
- then run 'git add .', 'git commit -m "message"', and 'git push -u origin main'
